bin/
*.db
*.db-wal
*.db-shm
.env
.omc
.omo

# Compiled binaries. Go build drops these beside the source, and they embed the
# absolute build path of whoever produced them.
/advisor-echo/advisor-echo
/relay/replay
/relay/shoulderd

# Phase 0 hook experiments. Kept on disk, out of the repository; what they produced
# and what still matters are the fixtures in testdata/hook-payloads/.
spikes/
